// Import this file once in your app entry so the scene is registered.
import '../scenes/requests-scene';

// Optional: force-switch to the scene for testing.
// setTimeout(() => { (window as any).__director?.requestScene?.('Requests Floaters'); }, 1000);